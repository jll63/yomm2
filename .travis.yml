language: cpp

before_install:
    - sudo apt-get -y install cmake

dist: bionic
sudo: required

compiler:
    - clang
    - gcc

env:
    - CONFIG=Debug
    - CONFIG=Release
    - HYBRID=ReleaseDebug
    - HYBRID=DebugRelease

notifications:
 email:
   on_success: change
   on_failure: always

script: dev/travis
